import * as React from "react";

const Map: React.VFC = () => {
    return (
        <div style={{backgroundColor: '#fff', outline: '1px solid red'}}>
            All the cases on an interactive map
        </div>
    );
};

export default Map;
