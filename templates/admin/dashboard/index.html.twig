{% extends 'admin/base.html.twig' %}

{% block title %}
    Dashboard
{% endblock %}

{% block main %}
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-12 col-md-10 col-lg-8 col-xl-7">
            <h1>Dashboard</h1>
            <div class="row">
                <div class="col-12 col-md-6 col-lg-4 mb-3">
                    <a href="{{ path('admin_entity_edit', {'entityName': 'cases'}) }}" class="card w-100 h-100">
                        <div class="card-body">
                            <h5 class="card-title">Edit Cases</h5>
                        </div>
                    </a>
                </div>
                <div class="col-12 col-md-6 col-lg-4 mb-3">
                    <a href="{{ path('admin_entity_edit', {'entityName': 'in-the-press'}) }}" class="card w-100 h-100">
                        <div class="card-body">
                            <h5 class="card-title">Edit "In the Press"</h5>
                        </div>
                    </a>
                </div>
                <div class="col-12 col-md-6 col-lg-4 mb-3">
                    <a href="{{ path('admin_entity_edit', {'entityName': 'screenings'}) }}" class="card w-100 h-100">
                        <div class="card-body">
                            <h5 class="card-title">Edit Screenings</h5>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}