<nav class="navbar navbar-dark navbar-expand-md sticky-top bg-dark justify-content-between">
    <a class="navbar-brand" href="{{ path('admin_dashboard') }}">The Bedechka Case</a>

    <div class="order-2">
        <div class="dropdown">
            <button class="avatar bg-{{ random_bs_color_name() }}"
                    type="button"
                    id="userMenu"
                    data-toggle="dropdown"
                    aria-haspopup="true"
                    aria-expanded="false">
                {{ app.user.email|first_letter|capitalize }}
            </button>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="userMenu">
                <h6 class="dropdown-header">
                    <span class="text-truncate">
                        {{ app.user.email }}
                    </span>
                </h6>
                <a class="dropdown-item"
                   href="{{ path('admin_security_change_password') }}">
                    Change Password
                </a>
                <a class="dropdown-item"
                   href="{{ logout_path() }}">
                    <strong>Logout</strong>
                </a>
            </div>
        </div>
    </div>
</nav>