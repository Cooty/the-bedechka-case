{% extends('admin/form-only.html.twig') %}

{% block main %}
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-12 col-md-8 col-lg-5 col-xl-4">
            <div class="card w-100">
                <div class="card-body">
                    <h5>404 Not found</h5>
                    <div class="alert alert-primary">
                        The page that you are looking for can't be found in the admin.
                        Try going back to the <a href="{{ path('admin_dashboard') }}">Dashboard</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}