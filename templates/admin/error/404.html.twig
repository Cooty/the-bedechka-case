{% extends('admin/form-only.html.twig') %}

{% block main %}
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-12 col-md-8 col-lg-5 col-xl-4">
            <div class="card w-100">
                <div class="card-body">
                    <h5>404 Not found</h5>
                    {% set dashBoardUri = path('admin_dashboard') %}
                    {% set message = "The page that you are looking for can't be found in the admin. Try going back to the <a href=\"#{dashBoardUri}\">Dashboard</a>" %}
                    {% include('admin/includes/alert.html.twig') with {
                        'message': message,
                        'label': 'primary'
                    } %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}