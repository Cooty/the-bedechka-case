{% if newsFirstPage|length > 0 %}
    <section class="story">
        {% include('/components/section-header/section-header.html.twig') with {
            'text': 'story.news.title'|trans,
            'classes': 'story__header'
        } only %}

        {% include('components/news/news-list.html.twig') with {
            'newsFirstPage': newsFirstPage,
            'newsHasPagination': newsHasPagination
        } only %}
    </section>
{% endif %}