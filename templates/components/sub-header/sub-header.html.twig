{% if text is defined %}
    {% set priority = priority is defined ? priority : 3 %}
    {% set classes = classes is defined ? classes : '' %}
    <h{{ priority }} class="sub-header {{ classes }}">
        {{ text|trans }}
    </h{{ priority }}>
{% endif %}