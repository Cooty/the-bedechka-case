{% if text is defined %}
    {% set priority = priority is defined ? priority : 2 %}
    {% set classes = classes is defined ? classes : '' %}
    <h{{ priority }} class="section-header {{ classes }}">
        {{ text|trans }}
    </h{{ priority }}>
{% endif %}