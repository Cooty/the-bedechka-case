{% extends 'base.html.twig' %}

{% set heroTitle = {'mainTitle': 'Organizations who...', 'subTitle': 'Support our cause'} %}
{% set heroContent = include('./includes/hero/hero-title-and-subtitle.html.twig', heroTitle) %}

{% set heroSettings = {
    'background': 'partners',
    'content': heroContent
} %}

{% block title %} Partners {% endblock %}

{% block main %}
    {% include('./includes/hero.html.twig') with heroSettings %}

    <div class="wrapper">
        <article class="box box--pull-up">
            <div class="grid">
                <ul class="grid__column grid__column--third">
                    <li>
                        <h2 class="box__header">
                            Stara Zagora
                        </h2>
                    </li>
                </ul>
                <ul class="grid__column grid__column--third">

                </ul>
                <ul class="grid__column grid__column--third">

                </ul>
            </div>
        </article>
    </div>
{% endblock %}